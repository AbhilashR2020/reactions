{application, reactions,
        [{description, "reactions as Like/Dislikes"},
         {vsn, "0.1.0"},
         {modules, [reaction_server,
                    reaction_mnesia,
                    reactions_i,
                    reactions_app,
                    reactions_sup,
                    fragmentation]},
         {registered, []},
         {applications, [kernel, stdlib, sasl, mnesia]},
         {mod, {reactions_app, []}}
]}.
